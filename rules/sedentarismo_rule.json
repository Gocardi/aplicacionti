{
  "id": "sedentarismo_rule",
  "sql": "SELECT max(value) - min(value) AS pasos_recientes FROM watch_stream WHERE sensor = 'steps' GROUP BY TumblingWindow(ss, 60) HAVING pasos_recientes < 5",
  "actions": [
    {
      "mqtt": {
        "server": "tcp://mosquitto:1883",
        "topic": "edge/acciones",
        "sendSingle": true
      }
    }
  ]
}
